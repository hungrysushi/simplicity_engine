#pragma once

#include <vector>

namespace simplicity {

class Newton {
public:
        Newton();
        ~Newton();
        SimpleDetectCollision(vector<vector<int>> a, vector<vector<int>> b)

private:
};

} /* namespace simplicity */
